# Supabase — authentication & cloud sync (Phase 7)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# ToPrep backend (toprep-llm-youtube) — YouTube search (Phase 3) &
# AI topic organization (Phase 2). The backend holds the YouTube/AI
# provider keys; the frontend only needs its base URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
